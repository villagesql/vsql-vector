name: test
on: [pull_request, workflow_dispatch]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: villagesql/extension-actions/cpp@main
        with:
          extension-name: vsql_vector
          github-token: ${{ secrets.GITHUB_TOKEN }}
